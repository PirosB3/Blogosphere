<%=render 'layouts/header'%>
<div class="span1 sign_in_column_one">
</div>

<% @unsent_checkouts.each do |checkout| %>
	<div class="span10">
		<div class="span2">
			<h3>Checkout_id: </h3>
		</div>
		<div class="span2">
			<%= link_to "#{checkout.stripe_transaction_id}", controller: 'administration/checkouts', action: "show", id: checkout %>
		</div>
		<div class="span2">
			<h3>User Email:</h3>
		</div>
		<div class="span2">
			<% user = User.where(:id => checkout.user_id) %>
			<%= user[0].email %>
		</div>
	<!-- #for each checkout create a link to the checkout and show as well as user email and no.of magazines to ship -->

	<!--  # create a link to the checkout as well as     (MAKE SURE YOU LOOK AT RAKE ROUTES) -->
	</div>
<%end%>


